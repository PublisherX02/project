FROM python:3.10-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY security_api.py .
# Expose only internally
EXPOSE 8000
CMD ["uvicorn", "security_api.py:app", "--host", "0.0.0.0", "--port", "8000"]
